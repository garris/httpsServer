Self-generated SSL cert for dev server only.

To generate:
    ./generate ‘your.fqdn.com’

To get green lock in dev:
  - double-click ca.cert.pem to add to keychain
  - drag it to the System keychain
  - double-click it and set it to trusted
